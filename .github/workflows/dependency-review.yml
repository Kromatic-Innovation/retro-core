name: Dependency Review

# Blocks a PR that introduces a vulnerable or disallowed-license dependency. The
# dependency-graph API it reads requires a public repo (or GHAS), so the job is
# guarded to run only when the repository is public. This repo is private today,
# so the job is inert; it activates by itself the moment visibility flips, with
# no edit needed here. See retro-core#15.

on:
  pull_request:
    branches: [develop]

permissions:
  contents: read

jobs:
  dependency-review:
    name: Dependency review
    runs-on: ubuntu-latest
    if: ${{ github.event.repository.visibility == 'public' }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Dependency review
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v4
        with:
          license-check: true
          vulnerability-check: true
